// Entry point for the NLDD Design System plugin-UI bundle (public/plugin-ui/nldd.js).
// Importing the package registers every <nldd-*> element as a side effect; plugin
// iframes load the built IIFE via loadNldd() to render real components.
import '@nldd/design-system';
