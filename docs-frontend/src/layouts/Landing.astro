---
import '@minbzk/storybook/styles';
import '../styles/global.css';

export interface Props {
  title?: string;
}

const { title = 'Fundament' } = Astro.props;

const { pathname } = Astro.url;
const isActive = (path: string) => pathname === path || pathname === `${path}/`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Fundament - Open-source platform for deploying and managing Kubernetes clusters with bare-metal provisioning"
    />
    <title>{title}</title>
    <link rel="icon" type="image/png" href="/img/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/img/favicon.svg" />
    <link rel="shortcut icon" href="/img/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/img/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
  </head>
  <body>
    <header class="topbar">
      <div class="topbar__inner">
        <a href="/" class="wordmark">
          <span class="wordmark__logo text-indigo-600 dark:text-indigo-400" aria-hidden="true">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
              <path
                fill="currentColor"
                d="M6 5h12a1 1 0 0 1 1 1a1 1 0 0 1-1 1H6a1 1 0 0 1-1-1a1 1 0 0 1 1-1m15-3v2H3V2zm-6 6h2v14h-2zM7 8h2v14H7zm4 0h2v14h-2z"
              ></path>
            </svg>
          </span>
          Fundament
        </a>
        <nav class="topnav" aria-label="Primary">
          <a href="/" aria-current={isActive('/') ? 'page' : undefined}>Home</a>
          <a href="/about" aria-current={isActive('/about') ? 'page' : undefined}>About</a>
          <a href="/docs" aria-current={isActive('/docs') ? 'page' : undefined}>Docs</a>
        </nav>
        <div class="topbar__spacer"></div>
        <nldd-icon-button id="theme-toggle" icon="moon" aria-label="Toggle theme" type="button"
        ></nldd-icon-button>
        <nldd-icon-button
          class="-ml-5.5"
          href="https://github.com/fundament-oss/fundament"
          target="_blank"
          rel="nofollow noopener noreferrer"
          aria-label="GitHub repository"
        >
          <svg
            slot="icon"
            class="h-5.5 w-5.5"
            fill="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              fill-rule="evenodd"
              d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"
              clip-rule="evenodd"></path>
          </svg>
        </nldd-icon-button>
      </div>
    </header>

    <slot />

    <footer class="foot">
      <div class="foot__inner">
        <span class="wordmark cursor-default">
          <span class="wordmark__logo" aria-hidden="true">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
              <path
                fill="currentColor"
                d="M6 5h12a1 1 0 0 1 1 1a1 1 0 0 1-1 1H6a1 1 0 0 1-1-1a1 1 0 0 1 1-1m15-3v2H3V2zm-6 6h2v14h-2zM7 8h2v14H7zm4 0h2v14h-2z"
              ></path>
            </svg>
          </span>
          Fundament
        </span>
        <div class="foot__links">
          <a href="https://digilab.overheid.nl/" target="_blank">Digilab</a>
          <a
            href="https://github.com/fundament-oss/fundament"
            target="_blank"
            rel="nofollow noopener noreferrer">GitHub</a
          >
        </div>
        <div class="foot__meta">Open source · Apache 2.0</div>
      </div>
    </footer>
  </body>
</html>

<script>
  import '@minbzk/storybook';
</script>

<script is:inline>
  /* eslint-disable */
  (function () {
    var htmlEl = document.documentElement;

    function apply(isDark) {
      htmlEl.dataset.scheme = isDark ? 'dark' : 'light';
      htmlEl.classList.toggle('dark', isDark);

      var btn = document.getElementById('theme-toggle');
      if (btn) btn.setAttribute('icon', isDark ? 'sun' : 'moon');
    }

    var saved = localStorage.getItem('starlight-theme') || localStorage.getItem('fnd-scheme');
    var isDark =
      saved === 'dark'
        ? true
        : saved === 'light'
          ? false
          : window.matchMedia('(prefers-color-scheme: dark)').matches;

    apply(isDark);

    document.getElementById('theme-toggle')?.addEventListener('click', function () {
      isDark = !isDark;
      localStorage.setItem('starlight-theme', isDark ? 'dark' : 'light');
      localStorage.setItem('fnd-scheme', isDark ? 'dark' : 'light');
      if (document.startViewTransition) {
        document.startViewTransition(function () {
          apply(isDark);
        });
      } else {
        apply(isDark);
      }
    });
  })();
</script>

<style is:global>
  @reference "tailwindcss/theme";

  body {
    font: var(--primitives-font-body-md-regular-snug);
    color: var(--semantics-content-color);
    background: var(--semantics-surfaces-background-color);
    @apply m-0 antialiased;
  }

  *:focus-visible {
    outline: var(--semantics-focus-ring-outline);
    box-shadow: var(--semantics-focus-ring-box-shadow);
  }

  a {
    color: var(--semantics-links-color);
    @apply underline decoration-1 underline-offset-[3px];
  }
  a:hover {
    color: var(--semantics-links-is-hovered-color);
  }

  /* Top bar */
  .topbar {
    border-bottom: 1px solid var(--semantics-dividers-color);
    background: var(--semantics-surfaces-background-color);
    @apply sticky top-0 z-100;
  }
  .topbar__inner {
    @apply mx-auto flex h-16 max-w-7xl items-center;
    padding: 0 var(--primitives-space-32);
    gap: var(--primitives-space-32);
  }
  .wordmark {
    font: var(--primitives-font-body-lg-bold-tight);
    color: var(--semantics-content-color);
    @apply inline-flex items-center gap-2.5 tracking-[-0.01em] no-underline;
  }
  .wordmark:hover {
    color: var(--semantics-content-color);
  }
  .wordmark__logo {
    @apply inline-flex h-5.5 w-5.5 items-center justify-center;
  }
  .wordmark__logo svg {
    @apply h-full w-full;
  }

  .topnav {
    @apply flex;
    gap: var(--primitives-space-24);
    margin-left: var(--primitives-space-16);
  }
  .topnav a {
    font: var(--primitives-font-body-md-regular-snug);
    color: var(--semantics-content-color);
    @apply px-0 py-1 no-underline;
  }
  .topnav a[aria-current='page'] {
    @apply font-medium;
    border-bottom: 2px solid var(--primitives-color-accent-600);
  }
  .topnav a:hover {
    color: var(--semantics-links-is-hovered-color);
  }
  .topbar__spacer {
    @apply flex-1;
  }
  /* Footer */
  .foot {
    border-top: 1px solid var(--semantics-dividers-color);
  }
  .foot__inner {
    @apply mx-auto flex max-w-7xl items-center justify-between;
    padding: var(--primitives-space-32);
    gap: var(--primitives-space-24);
  }
  .foot__links {
    @apply flex;
    gap: var(--primitives-space-24);
    font: var(--primitives-font-body-sm-regular-snug);
  }
  .foot__links a:hover {
    @apply no-underline;
  }
  .foot__meta {
    font: var(--primitives-font-body-sm-regular-snug);
    color: var(--semantics-content-secondary-color);
  }
</style>
