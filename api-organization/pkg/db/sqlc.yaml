version: "2"
sql:
  - engine: "postgresql"
    queries: "queries.sql"
    schema: "../../../db/fundament.sql"
    gen:
      go:
        package: "db"
        out: "gen"
        sql_package: "pgx/v5"
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
