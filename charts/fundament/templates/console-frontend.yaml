{{- if $.Values.consoleFrontend.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $.Release.Name }}-console-frontend-config
  labels:
    {{- include "fundament.labels" . | nindent 4 }}
    app.kubernetes.io/component: console-frontend
data:
  config.json: |
    {
      "authnApiUrl": "{{ $.Values.externalUrls.authn }}",
      "organizationApiUrl": "{{ $.Values.externalUrls.organization }}"
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $.Release.Name }}-console-frontend
  labels:
    {{- include "fundament.labels" . | nindent 4 }}
    app.kubernetes.io/component: console-frontend
spec:
  replicas: {{ $.Values.consoleFrontend.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: console-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/component: console-frontend
    spec:
      containers:
        - name: console-frontend
          image: {{ $.Values.images.consoleFrontend }}
          ports:
            - name: http
              containerPort: 80
          volumeMounts:
            - name: config
              mountPath: /usr/share/nginx/html/assets/config
              readOnly: true
            - name: config
              mountPath: /app/public/assets/config
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: {{ $.Release.Name }}-console-frontend-config
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-console-frontend
  labels:
    {{- include "fundament.labels" . | nindent 4 }}
    app.kubernetes.io/component: console-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/component: console-frontend
{{- if $.Values.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $.Release.Name }}-console-frontend
  labels:
    {{- include "fundament.labels" . | nindent 4 }}
    app.kubernetes.io/component: console-frontend
  {{- if $.Values.ingress.tls.clusterIssuer }}
  annotations:
    cert-manager.io/cluster-issuer: {{ $.Values.ingress.tls.clusterIssuer }}
  {{- end }}
spec:
  ingressClassName: {{ $.Values.ingress.className }}
  {{- if $.Values.ingress.tls.clusterIssuer }}
  tls:
    - hosts:
        - console.{{ $.Values.ingress.domain }}
      secretName: {{ $.Release.Name }}-console-frontend-tls
  {{- end }}
  rules:
    - host: console.{{ $.Values.ingress.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $.Release.Name }}-console-frontend
                port:
                  name: http
{{- end }}
{{- end }}
