{{- if $.Values.marketplaceRegistryFrontend.httproute.enabled }}
{{- if not $.Values.marketplaceRegistryFrontend.httproute.parentRefs }}{{ fail "marketplaceRegistryFrontend.httproute.parentRefs is required when httproute is enabled" }}{{ end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $.Release.Name }}-marketplace-registry-frontend
  labels:
    {{- include "fundament.labels" (dict "root" $ "name" "marketplace-registry-frontend" "component" "gateway") | nindent 4 }}
spec:
  parentRefs:
    {{- toYaml $.Values.marketplaceRegistryFrontend.httproute.parentRefs | nindent 4 }}
  hostnames:
    {{- toYaml $.Values.marketplaceRegistryFrontend.httproute.hostnames | nindent 4 }}
  rules:
    - backendRefs:
        - name: marketplace-registry-frontend
          port: 80
{{- end }}
