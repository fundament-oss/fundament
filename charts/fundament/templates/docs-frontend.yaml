{{- if $.Values.docsFrontend.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $.Release.Name }}-docs-frontend
  labels:
    {{- include "fundament.labels" . | nindent 4 }}
    app.kubernetes.io/component: docs-frontend
spec:
  replicas: {{ $.Values.docsFrontend.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: docs-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/component: docs-frontend
    spec:
      containers:
        - name: docs-frontend
          image: {{ $.Values.images.docsFrontend }}
          ports:
            - name: http
              containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-docs-frontend
  labels:
    {{- include "fundament.labels" . | nindent 4 }}
    app.kubernetes.io/component: docs-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/component: docs-frontend
---
{{- if $.Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $.Release.Name }}-docs-frontend
  labels:
    {{- include "fundament.labels" . | nindent 4 }}
    app.kubernetes.io/component: docs-frontend
spec:
  ingressClassName: {{ $.Values.ingress.className }}
  rules:
    - host: docs.{{ $.Values.ingress.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $.Release.Name }}-docs-frontend
                port:
                  name: http
{{- end }}
{{- end }}
