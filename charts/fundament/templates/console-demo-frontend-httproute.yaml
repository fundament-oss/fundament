{{- if $.Values.consoleDemoFrontend.httproute.enabled }}
{{- if not $.Values.consoleDemoFrontend.httproute.parentRefs }}{{ fail "consoleDemoFrontend.httproute.parentRefs is required when httproute is enabled" }}{{ end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $.Release.Name }}-console-demo-frontend
  labels:
    {{- include "fundament.labels" (dict "root" $ "name" "console-demo-frontend" "component" "gateway") | nindent 4 }}
spec:
  parentRefs:
    {{- toYaml $.Values.consoleDemoFrontend.httproute.parentRefs | nindent 4 }}
  hostnames:
    {{- toYaml $.Values.consoleDemoFrontend.httproute.hostnames | nindent 4 }}
  rules:
    - backendRefs:
        - name: console-demo-frontend
          port: 80
{{- end }}
