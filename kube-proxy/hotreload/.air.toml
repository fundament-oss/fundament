root = "/src"
tmp_dir = "tmp"

[build]
cmd = "go build -o tmp/fun-kube-proxy ./kube-proxy/cmd/fun-kube-proxy"
entrypoint = "tmp/fun-kube-proxy"
include_ext = ["go", "mod", "sum"]
include_dir = ["kube-proxy", "common"]
exclude_dir = ["tmp"]
delay = 500

[log]
time = false
