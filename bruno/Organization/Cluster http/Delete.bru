meta {
  name: Delete
  type: http
  seq: 5
}

post {
  url: {{organizationUrl}}/organization.v1.ClusterService/DeleteCluster
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
      "clusterId": "{{clusterId}}"
    }
}

settings {
  encodeUrl: true
  timeout: 0
}
