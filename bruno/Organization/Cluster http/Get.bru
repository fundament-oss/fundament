meta {
  name: Get
  type: http
  seq: 2
}

post {
  url: {{organizationUrl}}/organization.v1.ClusterService/GetCluster
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "clusterId": "{{clusterId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
