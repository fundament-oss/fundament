meta {
  name: Accept Invitation
  type: http
  seq: 3
}

post {
  url: {{organizationUrl}}/organization.v1.InviteService/AcceptInvitation
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "id": "{{invitationId}}"
  }
}
