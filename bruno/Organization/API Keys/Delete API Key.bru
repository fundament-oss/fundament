meta {
  name: Delete API Key
  type: http
  seq: 4
}

post {
  url: {{organizationUrl}}/organization.v1.APIKeyService/DeleteAPIKey
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "apiKeyId": "{{apiKeyId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
