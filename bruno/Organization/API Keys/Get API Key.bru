meta {
  name: Get API Key
  type: http
  seq: 3
}

post {
  url: {{organizationUrl}}/organization.v1.APIKeyService/GetAPIKey
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "apiKeyId": "{{apiKeyId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
