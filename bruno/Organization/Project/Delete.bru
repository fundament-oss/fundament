meta {
  name: Delete
  type: http
  seq: 5
}

post {
  url: {{organizationUrl}}/organization.v1.ProjectService/DeleteProject
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "projectId": "{{projectId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
