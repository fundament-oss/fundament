apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: test-dns
  namespace: fundament
spec:
  endpoints:
    - dnsName: test.fundament.local
      recordType: A
      targets:
        - 127.0.0.1
      recordTTL: 300
