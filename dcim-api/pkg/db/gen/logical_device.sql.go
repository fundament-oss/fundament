// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: logical_device.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const logicalDeviceCreate = `-- name: LogicalDeviceCreate :one
INSERT INTO dcim.logical_devices (logical_design_id, label, role, device_catalog_id, requirements, notes)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id
`

type LogicalDeviceCreateParams struct {
	LogicalDesignID uuid.UUID
	Label           string
	Role            string
	DeviceCatalogID pgtype.UUID
	Requirements    pgtype.Text
	Notes           pgtype.Text
}

func (q *Queries) LogicalDeviceCreate(ctx context.Context, arg LogicalDeviceCreateParams) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, logicalDeviceCreate,
		arg.LogicalDesignID,
		arg.Label,
		arg.Role,
		arg.DeviceCatalogID,
		arg.Requirements,
		arg.Notes,
	)
	var id uuid.UUID
	err := row.Scan(&id)
	return id, err
}

const logicalDeviceDelete = `-- name: LogicalDeviceDelete :execrows
UPDATE dcim.logical_devices
SET deleted = now()
WHERE id = $1 AND deleted IS NULL
`

type LogicalDeviceDeleteParams struct {
	ID uuid.UUID
}

func (q *Queries) LogicalDeviceDelete(ctx context.Context, arg LogicalDeviceDeleteParams) (int64, error) {
	result, err := q.db.Exec(ctx, logicalDeviceDelete, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const logicalDeviceGetByID = `-- name: LogicalDeviceGetByID :one
SELECT id, logical_design_id, label, role, device_catalog_id, requirements, notes, created
FROM dcim.logical_devices
WHERE id = $1 AND deleted IS NULL
`

type LogicalDeviceGetByIDParams struct {
	ID uuid.UUID
}

type LogicalDeviceGetByIDRow struct {
	ID              uuid.UUID
	LogicalDesignID uuid.UUID
	Label           string
	Role            string
	DeviceCatalogID pgtype.UUID
	Requirements    pgtype.Text
	Notes           pgtype.Text
	Created         pgtype.Timestamptz
}

func (q *Queries) LogicalDeviceGetByID(ctx context.Context, arg LogicalDeviceGetByIDParams) (LogicalDeviceGetByIDRow, error) {
	row := q.db.QueryRow(ctx, logicalDeviceGetByID, arg.ID)
	var i LogicalDeviceGetByIDRow
	err := row.Scan(
		&i.ID,
		&i.LogicalDesignID,
		&i.Label,
		&i.Role,
		&i.DeviceCatalogID,
		&i.Requirements,
		&i.Notes,
		&i.Created,
	)
	return i, err
}

const logicalDeviceList = `-- name: LogicalDeviceList :many
SELECT id, logical_design_id, label, role, device_catalog_id, requirements, notes, created
FROM dcim.logical_devices
WHERE logical_design_id = $1 AND deleted IS NULL
ORDER BY created
`

type LogicalDeviceListParams struct {
	LogicalDesignID uuid.UUID
}

type LogicalDeviceListRow struct {
	ID              uuid.UUID
	LogicalDesignID uuid.UUID
	Label           string
	Role            string
	DeviceCatalogID pgtype.UUID
	Requirements    pgtype.Text
	Notes           pgtype.Text
	Created         pgtype.Timestamptz
}

func (q *Queries) LogicalDeviceList(ctx context.Context, arg LogicalDeviceListParams) ([]LogicalDeviceListRow, error) {
	rows, err := q.db.Query(ctx, logicalDeviceList, arg.LogicalDesignID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []LogicalDeviceListRow
	for rows.Next() {
		var i LogicalDeviceListRow
		if err := rows.Scan(
			&i.ID,
			&i.LogicalDesignID,
			&i.Label,
			&i.Role,
			&i.DeviceCatalogID,
			&i.Requirements,
			&i.Notes,
			&i.Created,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const logicalDeviceUpdate = `-- name: LogicalDeviceUpdate :execrows
UPDATE dcim.logical_devices
SET label             = COALESCE($2, label),
    role              = COALESCE($3, role),
    device_catalog_id = COALESCE($4, device_catalog_id),
    requirements      = COALESCE($5, requirements),
    notes             = COALESCE($6, notes)
WHERE id = $1 AND deleted IS NULL
`

type LogicalDeviceUpdateParams struct {
	ID              uuid.UUID
	Label           pgtype.Text
	Role            pgtype.Text
	DeviceCatalogID pgtype.UUID
	Requirements    pgtype.Text
	Notes           pgtype.Text
}

func (q *Queries) LogicalDeviceUpdate(ctx context.Context, arg LogicalDeviceUpdateParams) (int64, error) {
	result, err := q.db.Exec(ctx, logicalDeviceUpdate,
		arg.ID,
		arg.Label,
		arg.Role,
		arg.DeviceCatalogID,
		arg.Requirements,
		arg.Notes,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
