// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: circuitbreaker.sql

package db

import (
	"context"
)

const authzOutboxLagSeconds = `-- name: AuthzOutboxLagSeconds :one
SELECT COALESCE(EXTRACT(EPOCH FROM (now() - MIN(authz.outbox.created))), 0)::float8
FROM authz.outbox
WHERE authz.outbox.processed IS NULL
  AND authz.outbox.status IN ('pending', 'retrying')
`

// Returns the age in seconds of the oldest unprocessed outbox entry,
// or 0 when no pending/retrying rows exist.
func (q *Queries) AuthzOutboxLagSeconds(ctx context.Context) (float64, error) {
	row := q.db.QueryRow(ctx, authzOutboxLagSeconds)
	var column_1 float64
	err := row.Scan(&column_1)
	return column_1, err
}
