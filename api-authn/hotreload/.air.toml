root = "/src"
tmp_dir = "tmp"

[build]
cmd = "go build -o tmp/fun-authn-api ./api-authn/cmd/fun-authn-api"
entrypoint = "tmp/fun-authn-api"
include_ext = ["go", "mod", "sum"]
include_dir = ["api-authn"]
exclude_dir = ["tmp"]
delay = 500

[log]
time = false

[misc]
clean_on_exit = true
