# This directory holds the laptop's secrets: SSH host keys, the outbound
# id_ed25519, and the harvested Wi-Fi connection. Gitignored — see ../.gitignore.
# Seeded by `just setup` (keeps existing files; generates any missing keys).
